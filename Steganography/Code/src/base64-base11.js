//This class converts between Base64 and Base11 via BigInts and Binary Strings
export class x64x11
{
    constructor()
    {
        //you shouldn't be instantiating this
    }

    /**
     * The function "to64" converts a base 11 string to a base 64 string.
     * @param base11str - The parameter "base11str" is a string representing a number in base 11.
     */
    static to64(base11str)
    {

    }

    /**
     * The function "to11" converts a base 64 string to a base 11 string.
     * @param base64str - The parameter "base64str" is a string representing a number in base 64.
     */
    static to11(base64str)
    {

    }
}